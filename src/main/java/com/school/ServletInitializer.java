package com.school;

import org.springframework.boot.builder.SpringApplicationBuilder;
import org.springframework.boot.web.servlet.support.SpringBootServletInitializer;

public class ServletInitializer  {
	
	// this is needed if we deploy a war file and need to do some configuration before the deployment
	
//	public class ServletInitializer extends SpringBootServletInitializer {

	/*
	 * @Override protected SpringApplicationBuilder
	 * configure(SpringApplicationBuilder application) { System.out.
	 * println("  ServletInitializer - SpringApplicationBuilder configure"); return
	 * application.sources(SchoolApplication.class); }
	 */

}
